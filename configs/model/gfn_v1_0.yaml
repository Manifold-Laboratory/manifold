# MANIFOLD v1.0: Unified Conscious Engine Config
# Convergence: Thermodynamics + Symmetries + Fractals

model:
  dim: 512
  depth: 8
  heads: 8
  rank: 64
  integrator_type: 'heun'
  use_scan: false

  # Cognitive Physics Suite (The Three Pillars)
  physics:
    # 1. Active Inference (Thermodynamics)
    active_inference:
      enabled: true
      reactive_curvature:
        enabled: true
        plasticity: 0.1
      singularities:
        enabled: true
        threshold: 0.85
        strength: 5.0
      dynamic_time:
        enabled: true
        range: [0.1, 5.0]
    
    # 2. Semantic Symmetries (Noether)
    symmetries:
      enabled: true
      isomeric_groups: [[0, 1], [2, 3], [4, 5], [6, 7]] # Balanced invariance
    
    # 3. Fractal Manifolds (Recursion)
    fractal:
      enabled: true
      threshold: 0.4
      alpha: 0.2
    
    stability:
      curvature_clamp: 10.0
